#!/bin/sh

# polybar mail module.
newmail="$(printf %4d $(du -a ~/.local/share/mail/*/INBOX/new/* 2>/dev/null | sed -n '$='))"
[[ "$newmail" = *"0"* ]] && printf "📪$newmail" || printf "📬$newmail"
