#!/bin/bash

choice=$(echo "Selection" | dmenu -p "Choose how to take a screenshot" -no-custom -i)
case $choice in
    "Selection" )
        scrot '%Y-%m-%d-%H%M%S_$wx$h.png' -selection -e 'mv $f ~/pictures/screenshots/';;
esac
