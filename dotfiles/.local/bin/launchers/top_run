#!/usr/bin/env bash

# if [[ -z $(glxinfo | grep vendor | grep -i nvidia) ]]; then
#     # nouveau driver, use default terminal
#     gotop_term=$TERMINAL
# else
#     # nvidia driver, use sakura for correct font display
#     gotop_term=sakura
# fi

which gotop && toggle-gotop && exit \
    || which htop && toggle-htop && exit \
    || which top && toggle-top && exit
